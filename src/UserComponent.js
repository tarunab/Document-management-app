import React, { useState, useEffect } from "react";
import axios from "axios";

const UserComponent = (page, rowsPerPage, filterData) => {

    // const [tableRange, setTableRange] = useState([0]);
    // const [slice, setSlice] = useState([]);
    // const [data, setData] = useState([]);

    // axios.get('https://frontend.apply.crosslend.dev/user/info.json')
    //     .then(response => {
    //         setData([...response.data.body.User]);
    //         formatData(response.data.documents);
    //     });

    // return { slice, range: tableRange };
};

export default UserComponent